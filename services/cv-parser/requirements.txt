# Runtime dependencies, pinned for reproducible builds.
# To upgrade: bump deliberately, then re-run `pytest` + `mypy` + `ruff check`.

# CPU-only PyTorch wheels live on a separate index; the +cpu torch pin below
# needs this to resolve (see the torch line for why we pin +cpu at all).
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi==0.139.0
uvicorn[standard]==0.50.0
python-multipart==0.0.32
pydantic==2.13.2
pydantic-settings==2.12.0

spacy==3.8.14
rapidfuzz==3.14.5

pdfplumber==0.11.10
python-docx==1.2.0

transformers==5.13.0
# CPU-only build: PyPI's default `torch` wheel bundles full CUDA/cuDNN/NCCL
# libraries (several GB) that this service never uses — it only ever runs
# BERT NER inference on CPU. The dedicated CPU index below is a fraction of
# the size. See Dockerfile for the matching --index-url.
torch==2.12.1+cpu

slowapi==0.1.10
prometheus-fastapi-instrumentator==8.0.2
