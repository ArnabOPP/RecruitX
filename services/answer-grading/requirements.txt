# Runtime dependencies, pinned for reproducible builds.
# To upgrade: bump deliberately, then re-run `pytest` + `mypy` + `ruff check`.

# CPU-only PyTorch wheels live on a separate index; the +cpu torch pin below
# needs this to resolve (see cv-parser's requirements.txt for the same
# reasoning — this service also only ever runs inference on CPU).
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi==0.139.0
uvicorn[standard]==0.50.0
pydantic==2.13.2
pydantic-settings==2.12.0
python-dotenv==1.2.1

sentence-transformers==5.6.0
transformers==5.13.0
torch==2.12.1+cpu
numpy==2.4.6

slowapi==0.1.10
prometheus-fastapi-instrumentator==8.0.2
redis==8.0.1
